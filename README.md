# BOM → DD1750 (Flask v4)

Fixes:
- Material/NSN treated as DIGITS (not text)
- Better BOM parsing across lines (LV blocks)
- Overlay columns aligned: UOI + quantities centered in correct columns
- Drops OH QTY = 0

Deploy on Railway via Dockerfile.
